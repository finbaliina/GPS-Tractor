pyserial==3.5
requests==2.32.5
Pillow>=10,<13
python-dotenv>=1,<2
Flask>=3,<4
numpy
opencv-python
shapely
pyproj
huggingface_hub
matplotlib

# PyTorch and SAM2 are installed separately because the correct PyTorch
# build depends on whether this machine uses an NVIDIA GPU. See README.md.
